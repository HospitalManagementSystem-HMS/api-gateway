name: CI
 
on:
  pull_request:
    branches: [master]
  push:
    branches: [master]
 
jobs:
  pipeline:
    uses: HospitalManagementSystem-HMS/Hospital-Management/.github/workflows/backend-template.yaml@dev
 
    with:
      service: "api-gateway"
      image: "ghcr.io/HospitalManagementSystem-HMS/user-service"
      sonar_key: "HospitalManagementSystem-HMS_api-gateway_8d7be679-30a9-4bdb-b1dc-cb4b7ecd0572"
      trigger: ${{ github.event_name }}
 
    secrets: inherit